package cli

const (
	browserCookiesFlagHelp = "use logged-in browser cookies for age-restricted videos (may trigger OS security prompts)"
	browserCookiesFormHelp = "Age-restricted videos via logged-in browser; may trigger OS security prompts"
)
